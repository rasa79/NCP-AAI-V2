# Secret for RAG Agent (base64 encoded values)
# Note: In production, use proper secret management (e.g., Sealed Secrets, Vault)

apiVersion: v1
kind: Secret
metadata:
  name: rag-agent-secrets
type: Opaque
data:
  # TODO: Add base64-encoded secrets
  # Example: echo -n "your-api-key" | base64
  
  # API Keys (placeholder - replace with actual values)
  NIM_API_KEY: ""  # Add your NIM API key (base64 encoded)
  OPENAI_API_KEY: ""  # Add your OpenAI API key if needed (base64 encoded)
